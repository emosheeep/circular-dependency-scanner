---
"circular-dependency-scanner": minor
---

Require Node.js 22.13 or newer, update runtime dependencies, and modernize the development toolchain with Oxlint and Oxfmt.

Generate declarations at `dist/index.d.ts` so the published `types` entry resolves correctly.
